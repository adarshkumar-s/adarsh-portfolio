(() => {
"use strict";
const KEY="adarsh-todos", LEGACY=["todos","todoList","todo-items"];
const $=id=>document.getElementById(id), form=$("todoForm"), input=$("todoInput"), list=$("todoList");
if(!form||!input||!list)return;
const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;
let filter="all",query="",sort="created",tasks=load();
const makeId=()=>crypto.randomUUID?.()||"todo-"+Date.now().toString(36)+"-"+Math.random().toString(36).slice(2);
function normalize(x,i){if(!x||typeof x!=="object")return null;const title=typeof x.title==="string"?x.title.trim().slice(0,240):"";if(!title)return null;const created=Number(x.createdAt)||Date.now()+i;return{id:String(x.id||makeId()),title,completed:Boolean(x.completed??x.done),priority:["low","normal","high"].includes(x.priority)?x.priority:"normal",category:typeof x.category==="string"?x.category.trim().slice(0,32):"",dueDate:typeof x.dueDate==="string"&&/^\d{4}-\d{2}-\d{2}$/.test(x.dueDate)?x.dueDate:"",createdAt:created,updatedAt:Number(x.updatedAt)||created,order:Number.isFinite(Number(x.order))?Number(x.order):i};}
function parse(raw){try{const d=JSON.parse(raw),a=Array.isArray(d)?d:Array.isArray(d?.todos)?d.todos:[];return a.map(normalize).filter(Boolean)}catch{return null}}
function load(){try{const raw=localStorage.getItem(KEY);if(raw!==null)return parse(raw)||[];for(const key of LEGACY){const legacy=localStorage.getItem(key);if(legacy===null)continue;const migrated=parse(legacy);if(migrated){localStorage.setItem(KEY,JSON.stringify(migrated));return migrated}}}catch{}return[]}
function save(){try{localStorage.setItem(KEY,JSON.stringify(tasks))}catch{}}
const rank=p=>p==="high"?0:p==="normal"?1:2;
function visible(){const q=query.trim().toLowerCase();return tasks.filter(t=>(filter==="all"||(filter==="active"&&!t.completed)||(filter==="completed"&&t.completed))&&(!q||[t.title,t.category,t.priority].some(v=>v.toLowerCase().includes(q)))).sort((a,b)=>sort==="oldest"?a.createdAt-b.createdAt:sort==="priority"?rank(a.priority)-rank(b.priority)||b.createdAt-a.createdAt:sort==="due"?(a.dueDate||"9999-99-99").localeCompare(b.dueDate||"9999-99-99")||b.createdAt-a.createdAt:sort==="alphabetical"?a.title.localeCompare(b.title):b.createdAt-a.createdAt)}
function dueText(v){if(!v)return"";const d=new Date(v+"T00:00:00");return Number.isNaN(d.getTime())?"":d.toLocaleDateString(undefined,{month:"short",day:"numeric"})}
function meta(t){const el=document.createElement("div");el.className="task-meta";if(t.priority!=="normal"){const p=document.createElement("span");p.className="priority "+t.priority;p.textContent=t.priority;el.append(p)}if(t.category){const c=document.createElement("span");c.textContent=t.category;el.append(c)}if(t.dueDate){const d=document.createElement("span"),over=!t.completed&&t.dueDate<new Date().toISOString().slice(0,10);d.className=over?"due overdue":"due";d.textContent=(over?"Overdue · ":"Due · ")+dueText(t.dueDate);el.append(d)}return el}
function editTask(li,t){if(li.classList.contains("editing"))return;li.classList.add("editing");const f=document.createElement("form");f.className="inline-editor";const title=Object.assign(document.createElement("input"),{value:t.title,maxLength:240});title.setAttribute("aria-label","Task title");const priority=document.createElement("select");["low","normal","high"].forEach(v=>{const o=new Option(v[0].toUpperCase()+v.slice(1),v);o.selected=v===t.priority;priority.add(o)});priority.setAttribute("aria-label","Priority");const category=Object.assign(document.createElement("input"),{value:t.category,maxLength:32,placeholder:"Category"});category.setAttribute("aria-label","Category");const due=Object.assign(document.createElement("input"),{type:"date",value:t.dueDate});due.setAttribute("aria-label","Due date");const ok=Object.assign(document.createElement("button"),{type:"submit",textContent:"Save"}),cancel=Object.assign(document.createElement("button"),{type:"button",textContent:"Cancel"});f.append(title,priority,category,due,ok,cancel);li.querySelector(".task-body").replaceChildren(f);f.addEventListener("submit",e=>{e.preventDefault();const value=title.value.trim();if(!value){title.focus();return}Object.assign(t,{title:value.slice(0,240),priority:priority.value,category:category.value.trim().slice(0,32),dueDate:due.value,updatedAt:Date.now()});save();render()});cancel.addEventListener("click",render);title.addEventListener("keydown",e=>{if(e.key==="Escape")render()});title.focus();title.select()}
function item(t){const li=document.createElement("li");li.className="todo-item"+(t.completed?" done":"");li.dataset.id=t.id;const check=Object.assign(document.createElement("button"),{type:"button",className:"check",textContent:t.completed?"✓":""});check.setAttribute("aria-label",t.completed?"Mark task active":"Complete task");const body=document.createElement("div");body.className="task-body";const title=document.createElement("span");title.className="task-title";title.textContent=t.title;body.append(title,meta(t));const edit=Object.assign(document.createElement("button"),{type:"button",className:"item-action edit",textContent:"Edit"}),del=Object.assign(document.createElement("button"),{type:"button",className:"item-action delete",textContent:"×"});edit.setAttribute("aria-label","Edit task");del.setAttribute("aria-label","Delete task");check.addEventListener("click",()=>{t.completed=!t.completed;t.updatedAt=Date.now();save();render()});edit.addEventListener("click",()=>editTask(li,t));del.addEventListener("click",()=>{const remove=()=>{tasks=tasks.filter(x=>x.id!==t.id);save();render()};if(reduced)remove();else{li.classList.add("removing");setTimeout(remove,180)}});li.append(check,body,edit,del);return li}
function render(){const shown=visible();list.replaceChildren(...shown.map(item));const empty=$("emptyState");empty.hidden=shown.length>0;empty.querySelector("strong").textContent=query||filter!=="all"?"No matching tasks":"No tasks here";const done=tasks.filter(t=>t.completed).length;$("totalCount").textContent=tasks.length;$("activeCount").textContent=tasks.length-done;$("completedCount").textContent=done;$("markAll").disabled=!tasks.length||done===tasks.length;$("clearCompleted").disabled=!done}
form.addEventListener("submit",e=>{e.preventDefault();const title=input.value.trim();if(!title){input.focus();return}const now=Date.now();tasks.push({id:makeId(),title:title.slice(0,240),completed:false,priority:$("priorityInput").value,category:$("categoryInput").value.trim().slice(0,32),dueDate:$("dueDateInput").value,createdAt:now,updatedAt:now,order:tasks.length});save();input.value="";$("categoryInput").value="";$("dueDateInput").value="";$("priorityInput").value="normal";render();input.focus()});
document.querySelectorAll(".filters button").forEach(btn=>btn.addEventListener("click",()=>{document.querySelectorAll(".filters button").forEach(b=>{b.classList.toggle("active",b===btn);b.setAttribute("aria-pressed",String(b===btn))});filter=btn.dataset.filter||"all";render()}));
$("searchInput").addEventListener("input",e=>{query=e.target.value;render()});$("sortSelect").addEventListener("change",e=>{sort=e.target.value;render()});$("markAll").addEventListener("click",()=>{tasks.forEach(t=>{t.completed=true;t.updatedAt=Date.now()});save();render()});$("clearCompleted").addEventListener("click",()=>{tasks=tasks.filter(t=>!t.completed);save();render()});
addEventListener("storage",e=>{if(e.key===KEY){const incoming=parse(e.newValue||"[]");if(incoming){tasks=incoming;render()}}});
addEventListener("keydown",e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==="k"){e.preventDefault();$("searchInput").focus()}if(e.key==="/"&&!["INPUT","TEXTAREA","SELECT"].includes(document.activeElement?.tagName)){e.preventDefault();$("searchInput").focus()}});
render();
})();